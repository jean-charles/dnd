package com.gayasystem.games.dnd.neuralnetwork;

import org.springframework.lang.NonNull;

import java.util.Collection;

public class MachineLearning {
//    private final Map<String, Input> inputs = new HashMap<>();
//    private final Map<String, Output> outputs = new HashMap<>();

    public MachineLearning(final Network network) {
    }

    public void learn(@NonNull final Collection<Collection<Double>> inputs, @NonNull final Collection<Collection<Double>> expected) {
//        var output = outputs.get(name);
//        var error = output.error(Double.valueOf(expected));
//        if (error == 0)
//            return true;
//        output.learn(Double.valueOf(expected));
    }
}
